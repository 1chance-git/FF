freqtrade==2026.7
statsmodels==0.14.6

# hermes/ operational tooling (freqtrade-client, psutil, and rich are
# already transitive dependencies of freqtrade; pinned explicitly here
# too since hermes imports them directly).
freqtrade-client==2026.7
click==8.4.2
rich==15.0.0
psutil==7.2.2
python-json-logger==4.1.0
